apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispatch-service
spec:
  template:
    spec:
      containers:
        - name: dispatch-service
          resources:
            limits:
              cpu: 300m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 64Mi
