apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dispatch-service
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: vroom-staging-strip-service-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /dispatch-service
            pathType: Prefix
            backend:
              service:
                name: dispatch-service
                port:
                  number: 8080
