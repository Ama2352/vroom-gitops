apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vroom

resources:
  - deployment.yaml
  - service.yaml
  - secrets/sealed-secret.yaml

labels:
  - pairs:
      app: ride-service
