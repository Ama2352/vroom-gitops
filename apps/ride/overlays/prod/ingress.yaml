apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ride-service
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: vroom-prod-strip-service-prefix@kubernetescrd,vroom-prod-retry@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /ride-service
            pathType: Prefix
            backend:
              service:
                name: ride-service
                port:
                  number: 8080
