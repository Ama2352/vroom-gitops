apiVersion: apps/v1
kind: Deployment
metadata:
  name: ride-service
spec:
  template:
    spec:
      containers:
        - name: ride-service
          resources:
            limits:
              cpu: 300m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 64Mi
