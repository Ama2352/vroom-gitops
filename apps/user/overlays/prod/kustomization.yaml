apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml
  - secrets/user-db-secrets.yaml   # SealedSecret for vroom-prod namespace

namespace: vroom-prod

patches:
  - path: patches/resource_limits.yaml
  - path: patches/replicas.yaml

labels:
  - pairs:
      env: prod

images:
  - name: vroom-mvp/user-service
    newName: ama2352/vroom-mvp-user-service
    newTag: latest   # Kargo manages this tag
