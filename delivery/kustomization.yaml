apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - project.yaml
  - warehouse.yaml
  - stages/dev.yaml
  - stages/staging.yaml
  - stages/prod.yaml
  - analysis/prometheus-checks.yaml
  # secrets/ is managed by vroom-secrets ArgoCD app (see vroom-gitops/secrets/)
  - project-config.yaml
