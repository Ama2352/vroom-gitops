apiVersion: kargo.akuity.io/v1alpha1
kind: ProjectConfig
metadata:
  name: vroom
  namespace: vroom
spec:
  promotionPolicies:
    - stage: dev
      autoPromotionEnabled: true
    - stage: staging
      autoPromotionEnabled: true
