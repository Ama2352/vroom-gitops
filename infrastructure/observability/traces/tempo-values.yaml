# Tempo Values — single-binary, local filesystem storage
# Tuned for 9GB K3s cluster — 24h trace retention

tempo:
  retention: 24h

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi

  storage:
    trace:
      backend: local
      local:
        path: /var/tempo/traces
      wal:
        path: /var/tempo/wal

  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"
        http:
          endpoint: "0.0.0.0:4318"
    jaeger:
      protocols:
        thrift_http:
          endpoint: "0.0.0.0:14268"

  persistence:
    enabled: true
    storageClassName: local-path
    size: 1Gi

# Jaeger-compatible query UI
# --query.base-path=/tempo ensures all assets load from /tempo/** path
# matching the Traefik ingress route in ingress.yaml
tempoQuery:
  enabled: true
  extraArgs:
    - --query.base-path=/tempo
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 64Mi
