apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: ai-health-check
  namespace: vroom
spec:
  args:
    - name: target-namespace
    - name: stabilization-wait
      value: "300"   # default; dev stage overrides to 60
  metrics:
    - name: ai-health-analysis
      provider:
        job:
          spec:
            template:
              spec:
                restartPolicy: Never
                containers:
                  - name: reporter
                    image: docker.io/ama2352/vroom-mvp-ai-reporter:latest
                    imagePullPolicy: Always
                    args: ["--mode=verify"]
                    resources:
                      requests:
                        cpu: 50m
                        memory: 64Mi
                      limits:
                        cpu: 200m
                        memory: 128Mi
                    env:
                      - name: GEMINI_API_KEY
                        valueFrom:
                          secretKeyRef:
                            name: ai-reporter-secret
                            key: gemini-api-key
                      - name: SLACK_WEBHOOK_URL
                        valueFrom:
                          secretKeyRef:
                            name: ai-reporter-secret
                            key: slack-webhook-url
                      - name: PROMETHEUS_URL
                        value: "http://kube-prometheus-stack-prometheus.monitoring:9090/prometheus"
                      - name: LOKI_URL
                        value: "http://loki-stack.monitoring:3100"
                      - name: TARGET_NAMESPACE
                        value: "{{args.target-namespace}}"
                      - name: VERIFY_WINDOW
                        value: "10m"
                      - name: STABILIZATION_WAIT_SECONDS
                        value: "{{args.stabilization-wait}}"
      successCondition: result.exitCode == 0
      failureLimit: 1
