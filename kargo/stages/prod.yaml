apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod
  namespace: vroom
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: vroom-warehouse
      sources:
        stages: [staging]   # only Freight that passed staging verification
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/Ama2352/vroom-gitops.git
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          config:
            path: repo/apps/user/overlays/prod
            images:
              - image: vroom-mvp/user-service
                newName: ama2352/vroom-mvp-user-service
                digest: ${{ imageFrom("docker.io/ama2352/vroom-mvp-user-service").digest }}
        - uses: kustomize-set-image
          config:
            path: repo/apps/ride/overlays/prod
            images:
              - image: vroom-mvp/ride-service
                newName: ama2352/vroom-mvp-ride-service
                digest: ${{ imageFrom("docker.io/ama2352/vroom-mvp-ride-service").digest }}
        - uses: kustomize-set-image
          config:
            path: repo/apps/dispatch/overlays/prod
            images:
              - image: vroom-mvp/dispatch-service
                newName: ama2352/vroom-mvp-dispatch-service
                digest: ${{ imageFrom("docker.io/ama2352/vroom-mvp-dispatch-service").digest }}
        - uses: kustomize-set-image
          config:
            path: repo/apps/notification/overlays/prod
            images:
              - image: vroom-mvp/notification-service
                newName: ama2352/vroom-mvp-notification-service
                digest: ${{ imageFrom("docker.io/ama2352/vroom-mvp-notification-service").digest }}
        - uses: kustomize-set-image
          config:
            path: repo/apps/frontend/overlays/prod
            images:
              - image: vroom-mvp/frontend
                newName: ama2352/vroom-mvp-frontend
                digest: ${{ imageFrom("docker.io/ama2352/vroom-mvp-frontend").digest }}
        - uses: git-commit
          as: commit
          config:
            path: ./repo
            message: "chore(gitops): promote images to prod"
        - uses: git-push
          config:
            path: ./repo
        - uses: argocd-update
          config:
            apps:
              - name: vroom-prod-user
                namespace: argocd
                sources:
                  - repoURL: https://github.com/Ama2352/vroom-gitops.git
                    desiredRevision: ${{ outputs.commit.commit }}
              - name: vroom-prod-ride
                namespace: argocd
                sources:
                  - repoURL: https://github.com/Ama2352/vroom-gitops.git
                    desiredRevision: ${{ outputs.commit.commit }}
              - name: vroom-prod-dispatch
                namespace: argocd
                sources:
                  - repoURL: https://github.com/Ama2352/vroom-gitops.git
                    desiredRevision: ${{ outputs.commit.commit }}
              - name: vroom-prod-notification
                namespace: argocd
                sources:
                  - repoURL: https://github.com/Ama2352/vroom-gitops.git
                    desiredRevision: ${{ outputs.commit.commit }}
              - name: vroom-prod-frontend
                namespace: argocd
                sources:
                  - repoURL: https://github.com/Ama2352/vroom-gitops.git
                    desiredRevision: ${{ outputs.commit.commit }}
  # Prod requires human approval via `kargo approve` before promotion runs.
  # Verification fires automatically after the promotion completes.
  # A CRIT result fires Slack alerts but does NOT auto-rollback.
  verification:
    analysisTemplates:
      - name: prometheus-checks
      - name: ai-health-check
    args:
      - name: target-namespace
        value: vroom-prod
