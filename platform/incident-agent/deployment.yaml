apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-agent
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: incident-agent
  template:
    metadata:
      labels:
        app: incident-agent
    spec:
      containers:
      - name: agent
        image: vroom-mvp/incident-agent:latest
        ports:
        - containerPort: 5002
        env:
        - name: REDIS_URL
          value: "redis://redis.platform.svc.cluster.local:6379"
        - name: KUBECTL_EXECUTOR_URL
          value: "http://kubectl-executor.monitoring.svc.cluster.local:5001"
        - name: PROMETHEUS_URL
          value: "http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090/prometheus/api/v1/query"
        - name: LOKI_URL
          value: "http://loki-stack.monitoring.svc.cluster.local:3100/loki/api/v1/query_range"
        - name: TEMPO_URL
          value: "http://tempo.monitoring.svc.cluster.local:3100"
        - name: GITHUB_REPO
          value: "Ama2352/vroom-services"
        - name: DOCS_DIR
          value: "/docs"
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: openrouter-secret
              key: api-key
              optional: true
        - name: GROQ_API_KEY
          valueFrom:
            secretKeyRef:
              name: groq-secret
              key: api-key
              optional: true
        - name: LLM_MOCK
          value: "false"
        - name: LLM_MOCK_SCENARIO
          value: "scale_to_zero"
        - name: EXECUTOR_API_KEY
          valueFrom:
            secretKeyRef:
              name: kubectl-executor-secret
              key: EXECUTOR_API_KEY
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: github-token-secret
              key: GITHUB_TOKEN
        - name: GITHUB_GITOPS_REPO
          value: "Ama2352/vroom-gitops"
        resources:
          requests:
            memory: "200Mi"
            cpu: "100m"
          limits:
            memory: "400Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 120
          periodSeconds: 30
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: incident-agent
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: incident-agent
  ports:
  - port: 5002
    targetPort: 5002
    nodePort: 30081
